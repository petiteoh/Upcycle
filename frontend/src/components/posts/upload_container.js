import { connect } from "react-redux";
import Upload from "./upload";

const msp = (state) => {
  return {
  };
};

const mdp = (dispatch) => {
  return {
  };
};

export default connect(msp, mdp)(Upload);
